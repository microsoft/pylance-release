# In a multiroot workspace (open `testing/test.code-workspace`), this should find the 日本 module (Kanji for Nihon)
# otherwise if you just open the single folder, there should be an import resolution error below

import 日本
日本.