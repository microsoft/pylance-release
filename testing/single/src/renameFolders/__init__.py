# confirm this is updated properly when `renameFiles.py` is renamed
from renameFiles import RenameFileType

# confirm this is updated properly when `renameByModule.py` is renamed
from . import renameByModule

# confirm this is updated properly when `moveFile.py` is moved
from .. import moveFile